{
  "name": "tournamnt-firestore-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "mocha --reporter spec --timeout 20000"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.0",
    "firebase-admin": "^11.0.0",
    "mocha": "^10.0.0",
    "chai": "^4.3.0"
  }
}