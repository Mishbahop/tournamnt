{
  "name": "tournamnt-functions",
  "private": true,
  "engines": {
    "node": "18"
  },
  "scripts": {
    "start": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions",
    "lint": "eslint . || true"
  },
  "dependencies": {
    "firebase-admin": "^11.0.0",
    "firebase-functions": "^4.0.0"
  }
}